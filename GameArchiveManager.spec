# -*- mode: python ; coding: utf-8 -*-
"""Reproducible Windows onedir build; external archive tools are excluded."""

from version import APP_NAME, APP_VERSION, BUILD_TYPE


if BUILD_TYPE != "Release":
    raise RuntimeError("Official build requires BUILD_TYPE='Release'")

release_directory = f"{APP_NAME}-{APP_VERSION}"

a = Analysis(
    ["main.py"],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name=APP_NAME,
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)

coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name=release_directory,
)
